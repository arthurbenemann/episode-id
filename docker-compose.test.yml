services:
  tests:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: []
    command: >
      sh -c "pip install --no-cache-dir '.[dev]' && pytest tests/ -v"
    working_dir: /app
    volumes:
      - ./tests:/app/tests:ro
      - ./app:/app/app:ro
